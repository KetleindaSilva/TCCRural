<!DOCTYPE html>
<html>
<head>
    <title>Meus Anúncios</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.min.js"></script>
    <link href="/style/style.css" rel="stylesheet">
</head>
<body>
    <% if (anuncios.length > 0) { %>
        <h1 class="mt-3">Meus Anúncios</h1>
        <div class="row">
            <% anuncios.forEach(function(anuncio) { %>
                <div class="col-md-3">
                    <div class="card mb-4 text-white">
                        <img src="/<%= anuncio.imagem %>" alt="<%= anuncio.titulo %>" style="height: 40vh;" />
                        <div class="card-body text-center">
                            <h5 class="card-title">
                                <%= anuncio.titulo %>
                            </h5>
                            <a href="/anuncio/<%= anuncio.id %>" class="btn text-white btn1">Ver Detalhes</a>
                            <a href="/editar-anuncio/<%= anuncio.id %>" class="btn text-white btn1">Editar</a>
                            <a href="/excluir-anuncio/<%= anuncio.id %>" class="btn text-white btn-danger">Excluir</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    <% } else { %>
        <h1>Você ainda não cadastrou anúncios. Clique em "Criar Anúncio" para começar!</h1>
    <% } %>
</body>
</html>
